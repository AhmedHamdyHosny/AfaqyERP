@model Afaqy_Store.DataLayer.DeviceStatus

@{
    ViewBag.Title = Resources.Store.DeviceStatusDetails;
    Layout = "~/Views/Shared/_Popup.cshtml";
}

<div class="modal-header">
    <button type="button" class="close" ng-click="cancel()" aria-hidden="true">&times;</button>
    <h2 class="modal-title">@Resources.Store.DeviceStatusDetails</h2>
</div>

@using (Html.BeginForm())
{
    <div class="modal-body">
        <dl class="dl-horizontal">
            <dt>
               @Resources.Store.DeviceStatusNameEn
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DeviceStatus_en)
            </dd>
            <dt>
               @Resources.Store.DeviceStatusNameAr
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DeviceStatus_ar)
            </dd>
        </dl>
    </div>
    <div class="modal-footer">
        <button class="btn btn-success" type="button" ng-click="cancel()">@Resources.Resource.ButtonOk</button>
    </div>
}
